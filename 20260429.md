# Linux: Linha de Comando (Básico)

No mundo DevOps, o terminal não é apenas uma ferramenta, é o seu ambiente de trabalho principal. Nesta aula, vamos focar em comandos de **sobrevivência** e **visualização**. O objetivo é que você saiba exatamente onde está, consiga criar seus próprios arquivos e saiba filtrar informações em meio a grandes volumes de dados.

---

## 1. Localização e Visualização (Onde estou e o que vejo?)

Antes de sair correndo pelo servidor, você precisa saber se localizar.

* **`pwd` (Print Working Directory):** Exibe o caminho completo da pasta onde você está agora. Use sempre que se sentir perdido.
* **`ls` (List):** Lista os arquivos e pastas do diretório atual.
    * `ls -l`: Mostra detalhes como tamanho, data e permissões.
    * `ls -a`: Mostra arquivos ocultos (aqueles que começam com um ponto `.`).
* **`cd` (Change Directory):** Muda de pasta. 
    * `cd ..` volta um nível.
    * `cd ~` volta para sua pasta pessoal (home).

---

## 2. Criação e Manipulação (Construindo seu ambiente)

Agora que você sabe navegar, vamos aprender a criar elementos no sistema.

* **`mkdir` (Make Directory):** Cria uma nova pasta. 
    * Exemplo: `mkdir projetos`
* **`touch`:** Cria um arquivo vazio ou atualiza a data de um arquivo existente.
    * Exemplo: `touch notas.txt`
* **`cat` (Concatenate):** Exibe todo o conteúdo de um arquivo diretamente na tela. É ótimo para arquivos pequenos.
    * Exemplo: `cat /etc/os-release`

---

## 3. Edição de Arquivos com Nano

O **`nano`** é um editor de texto simples e amigável que roda dentro do terminal. Ao contrário de editores complexos, ele exibe os comandos principais no rodapé da tela.

* Para abrir ou criar um arquivo: `nano meu_arquivo.txt`
* **Comandos importantes dentro do Nano:**
    * `Ctrl + O`: Salva o arquivo (Write Out).
    * `Ctrl + X`: Sai do editor.

---

## 4. Filtragem e Extração de Dados (Filtro e Corte)

Em servidores, muitas vezes precisamos encontrar uma agulha no palheiro. Para isso, usamos o `grep` e o `cut`.

* **`grep` (Global Regular Expression Print):** Funciona como um filtro de buscas. Ele varre um arquivo e mostra apenas as linhas que contêm a palavra que você digitou.
    * Exemplo: `grep "aluno" /etc/passwd` (Busca apenas a linha do usuário aluno).
* **`cut` (Corte):** Serve para extrair colunas específicas de um arquivo de texto. 
    * No arquivo `/etc/passwd`, os dados são separados por dois pontos `:`.
    * Exemplo: `cut -d ":" -f 1 /etc/passwd` (O comando diz: use o delimitador `:` e me mostre apenas a primeira coluna, que são os nomes dos usuários).

---

## Desafio de Sobrevivência: O Filtro Gigante

Imagine que o arquivo `/etc/passwd` tem 500 usuários, mas você só precisa saber o nome de um usuário específico e nada mais. 

1. Primeiro, você usa o **`grep`** para isolar a linha desse usuário.
2. Depois, você poderia usar o **`cut`** para exibir apenas o nome dele, limpando as informações de ID e pastas que não interessam no momento.

---

## Exercícios de Fixação

*Responda às perguntas abaixo utilizando apenas as informações apresentadas nesta aula.*

1.  Se você acabou de acessar o servidor e não sabe em qual diretório está, qual comando deve digitar para ver o caminho completo na tela?
2.  Qual a diferença visual entre o comando `ls` e o comando `ls -a`?
3.  Você precisa criar uma pasta chamada "seguranca". Qual o comando exato?
4.  Para criar um arquivo vazio chamado "config.ini", qual comando você utiliza?
5.  Qual comando é utilizado para ler o conteúdo de um arquivo diretamente no terminal, sem precisar abrir um editor?
6.  Dentro do editor **nano**, qual a combinação de teclas você pressiona para sair do programa?
7.  Se você quiser procurar a palavra "erro" dentro de um arquivo de logs imenso, qual comando de filtragem você deve usar?
8.  Explique para que serve o parâmetro `-d` no comando `cut`.
9.  Como o comando `cut` pode ajudar na visualização do arquivo `/etc/passwd`?
10. Você está dentro da pasta `/var/log` e quer voltar imediatamente para a sua "casa" (diretório home). Qual o comando mais curto para fazer isso?
